header:
  - titles:
      # @start locale config
      en      : &EN       Archive
      # @end locale config
    url: /archive.html
  - titles:
      # @start locale config
      en      : &EN       Uses
      # @end locale config
    url: /uses.html

  - titles:
      # @start locale config
      en      : &EN       About
      # @end locale config
    url: /about.html
